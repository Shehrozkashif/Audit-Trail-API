# Core dependencies
setuptools>=42
wheel
twine

# API and web-related
flask
requests

# Logging and utilities
loguru

# Optional: for testing
pytest

# Optional: for JSON handling and validation (if used)
jsonschema
